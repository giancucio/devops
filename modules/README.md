# Modules

Reusable Infrastructure as Code and automation modules.

```text
modules/
├── azure/
└── aws/
```

Azure modules mainly use Bicep and Azure pipeline YAML.  
AWS modules mainly use Terraform and pipeline YAML.
