version: 0.2

phases:
  install:
    commands:
      - echo Installing Terraform
  build:
    commands:
      - terraform init
      - terraform fmt -check -recursive
      - terraform validate
      - terraform plan
