# ai/speech-service

This Terraform module was generated as a skeleton from the corresponding Bicep module.

## Variables

- `accountName` (string) (default: speech-portfolio)
- `location` (string) (default: eastus)

## Outputs

- `serviceId` (string)

## Example

```hcl
module "ai_speech-service" {
  source = "../../../../infrastructure/terraform/azure/ai/speech-service"
  # set variables here
}
```
