# ai/language-service

This Terraform module was generated as a skeleton from the corresponding Bicep module.

## Variables

- `accountName` (string) (default: langsvc-portfolio)
- `location` (string) (default: eastus)

## Outputs

- `resourceId` (string)

## Example

```hcl
module "ai_language-service" {
  source = "../../../../infrastructure/terraform/azure/ai/language-service"
  # set variables here
}
```
