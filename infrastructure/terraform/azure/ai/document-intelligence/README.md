# ai/document-intelligence

This Terraform module was generated as a skeleton from the corresponding Bicep module.

## Variables

- `accountName` (string) (default: docintel-portfolio)
- `location` (string) (default: eastus)

## Outputs

- `resourceId` (string)

## Example

```hcl
module "ai_document-intelligence" {
  source = "../../../../infrastructure/terraform/azure/ai/document-intelligence"
  # set variables here
}
```
