# security/defender-for-cloud

This Terraform module was generated as a skeleton from the corresponding Bicep module.

## Variables

- `pricingTier` (string) (default: Standard)

## Outputs

- `pricingId` (string)

## Example

```hcl
module "security_defender-for-cloud" {
  source = "../../../../infrastructure/terraform/azure/security/defender-for-cloud"
  # set variables here
}
```
