trigger: none

pr: none

name: aks-terraform-$(Date:yyyyMMdd)$(Rev:.r)

variables:
  - name: terraformVersion
    value: '1.15.8'

  - name: workingDirectory
    value: 'projects/azure-terraform-aks/terraform'

  - name: serviceConnection
    value: 'giancucio3cscwif'

  - name: backendResourceGroupName
    value: 'giancucio-tfstate-rg'

  - name: backendStorageAccountName
    value: 'giancuciotfstatesa'

  - name: backendContainerName
    value: 'tfstate'

  - name: backendKey
    value: 'aks.tfstate'

  - name: tfvarsFile
    value: 'terraform.tfvars'

pool:
  vmImage: ubuntu-latest

stages:
  - stage: Plan
    displayName: Terraform Plan
    jobs:
      - job: Plan
        displayName: Terraform Plan
        steps:
          - checkout: self

          - task: TerraformInstaller@1
            displayName: Install Terraform
            inputs:
              terraformVersion: $(terraformVersion)

          - task: AzureCLI@2
            displayName: Terraform Init
            inputs:
              azureSubscription: $(serviceConnection)
              scriptType: bash
              scriptLocation: inlineScript
              workingDirectory: $(workingDirectory)
              inlineScript: |
                terraform init \
                  -backend-config="resource_group_name=$(backendResourceGroupName)" \
                  -backend-config="storage_account_name=$(backendStorageAccountName)" \
                  -backend-config="container_name=$(backendContainerName)" \
                  -backend-config="key=$(backendKey)"

          - task: AzureCLI@2
            displayName: Terraform Fmt Check
            inputs:
              azureSubscription: $(serviceConnection)
              scriptType: bash
              scriptLocation: inlineScript
              workingDirectory: $(workingDirectory)
              inlineScript: |
                terraform fmt -check -recursive

          - task: AzureCLI@2
            displayName: Terraform Validate
            inputs:
              azureSubscription: $(serviceConnection)
              scriptType: bash
              scriptLocation: inlineScript
              workingDirectory: $(workingDirectory)
              inlineScript: |
                terraform validate

          - task: AzureCLI@2
            displayName: Terraform Plan
            inputs:
              azureSubscription: $(serviceConnection)
              scriptType: bash
              scriptLocation: inlineScript
              workingDirectory: $(workingDirectory)
              inlineScript: |
                if [ ! -f "$(tfvarsFile)" ]; then
                  echo "ERROR: $(tfvarsFile) was not found in $(workingDirectory)."
                  echo "If this file is ignored locally, commit a non-secret tfvars file (or use pipeline variables) for CI/CD."
                  exit 1
                fi

                terraform plan \
                  -var-file="$(tfvarsFile)" \
                  -out=tfplan

          - task: PublishPipelineArtifact@1
            displayName: Publish tfplan
            inputs:
              targetPath: '$(workingDirectory)/tfplan'
              artifact: 'tfplan'
              publishLocation: 'pipeline'

  - stage: Apply
    displayName: Terraform Apply
    dependsOn: Plan
    condition: and(succeeded(), eq(variables['Build.SourceBranch'], 'refs/heads/main'))
    jobs:
      - job: WaitForApproval
        displayName: Manual Approval
        pool: server
        timeoutInMinutes: 1440
        steps:
          - task: ManualValidation@0
            timeoutInMinutes: 1440
            inputs:
              notifyUsers: ''
              instructions: 'Review Terraform plan output artifact before approving apply.'
              onTimeout: 'reject'

      - job: Apply
        displayName: Terraform Apply
        dependsOn: WaitForApproval
        steps:
          - checkout: self

          - task: TerraformInstaller@1
            displayName: Install Terraform
            inputs:
              terraformVersion: $(terraformVersion)

          - task: DownloadPipelineArtifact@2
            displayName: Download tfplan
            inputs:
              artifact: 'tfplan'
              path: '$(workingDirectory)'

          - task: AzureCLI@2
            displayName: Terraform Init
            inputs:
              azureSubscription: $(serviceConnection)
              scriptType: bash
              scriptLocation: inlineScript
              workingDirectory: $(workingDirectory)
              inlineScript: |
                terraform init \
                  -backend-config="resource_group_name=$(backendResourceGroupName)" \
                  -backend-config="storage_account_name=$(backendStorageAccountName)" \
                  -backend-config="container_name=$(backendContainerName)" \
                  -backend-config="key=$(backendKey)"

          - task: AzureCLI@2
            displayName: Terraform Apply
            inputs:
              azureSubscription: $(serviceConnection)
              scriptType: bash
              scriptLocation: inlineScript
              workingDirectory: $(workingDirectory)
              inlineScript: |
                terraform apply -auto-approve tfplan
